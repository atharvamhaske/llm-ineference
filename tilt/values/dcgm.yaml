nodeSelector:
  nvidia.com/gpu.present: "true"
tolerations:
  - key: llm-d.ai/role
    operator: Exists
    effect: NoSchedule
serviceMonitor:
  enabled: true
  additionalLabels:
    release: kube-prometheus-stack
